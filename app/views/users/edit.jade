extends ../shared/template
block content
  body#editProfile
    .row
      .col-xs-12.col-md-4
        form(role='form', method='post', action='/profile')
          input(type='hidden', name='_method', value='put')
          .form-group
            label(for='name') Age
            input.form-control#name(type='text', name='age', value=user.age, placeholder = 'your age',  autofocus=true)
          .form-group
            label(for='email') Email
            input.form-control#email(type='email', name='email', value=user.email)
          .form-group
            label(for='photo') Photo
            input.form-control#photo(type='text', name='photo', value=user.photo)

          button.btn.btn-primary(type='submit') Update Profile

     .row
       .col-xs-12.col-md-12

block scripts
